import React from 'react';
import './logo.css';

const Logo = ({ className }) => {
    return (
        <>
            <h1 className={className}>
                <span>t</span>
                <span>:</span>
                <span>l</span>
                <span>k</span>
            </h1>
        </>
    );
};

export default Logo;
